import {FC} from 'react';
import {SliderCss} from '../../components/slider/sliderCss';

export const SlidersComponent: FC = () => {
  return(
      <>
        <h1>Sliders</h1>
        <SliderCss />
      </>
  );
}